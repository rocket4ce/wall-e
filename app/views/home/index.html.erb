<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<% @comments.each do |c| %>
  <%= c.user.email %>
  <%= c.user.username %>
  <p>
    <%= c.body %>
  </p>
<% end %>

<%= form_for @new_comment do |f| %>
  <%= f.text_area :body %>
  <%= f.submit %>
<% end %>
